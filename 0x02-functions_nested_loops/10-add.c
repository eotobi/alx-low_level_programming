#include "main.h"

/**
 * add - a function that adds two integers and returns the result
 * @one: the first number input
 * @two: the second number input
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
